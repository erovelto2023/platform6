import { US_STATE_FACTS } from "../lib/utils/state-facts";

console.log("Keys in US_STATE_FACTS:", Object.keys(US_STATE_FACTS));
console.log("Count:", Object.keys(US_STATE_FACTS).length);

process.exit(0);
